---
const currentYear = new Date().getFullYear();
---

<footer>Blog It - {currentYear}</footer>

<style>
  footer {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 70px;
    background-color: var(--primary);
    color: var(--accent);
  }
</style>
